import Ember from 'ember';

export default Ember.Controller.extend({
  /** A CSS identifier selector.
   *
   * Must not include the hash/pound at the start.
   *
   * @type {String}
   */
  systemVizSelector: 'system-system-ridership-viz'
});
